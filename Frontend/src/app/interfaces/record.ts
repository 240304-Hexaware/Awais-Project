export interface Record {
  id: string;
  taskID: string;
  description: string;
  priority: string;
  dueDate: string;
  assignee: string;
  user: string;
  parseFileName: string;
  parseFileId: string;
  specificationName: string;
  specificationId: string;
}
